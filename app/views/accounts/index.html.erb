<p id="notice"><%= notice %></p>

<h1>Accounts</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Name</th>
      <th>Interest Rate</th>
      <th>Balance</th>
      <th>Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td><%= react_component("AccountCard", account) %></td>
        <td><%= react_component("AccountCard2") %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p>BREAK</p>
<%= react_component('Hello', name: 'a component rendered from a view') %>
<p> end break</p>

<%= link_to 'New Account', new_account_path %>
