<%= simple_form_for(@parent_child) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :parent %>
    <%= f.association :child %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
