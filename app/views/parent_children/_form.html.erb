<%= form_with(model: parent_child, local: true) do |form| %>
  <% if parent_child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent_child.errors.count, "error") %> prohibited this parent_child from being saved:</h2>

      <ul>
      <% parent_child.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.text_field :parent_id, id: :parent_child_parent_id %>
  </div>

  <div class="field">
    <%= form.label :child_id %>
    <%= form.text_field :child_id, id: :parent_child_child_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
